export interface DashboardStats {
  ventas_dia: string;
  cambio_vs_ayer: string;
  ganancia_dia: string;
  gastos_hoy: string;
  margen_dia: number;
  fiado_pendiente_total: string;
  clientes_fiado_pendiente: number;
  productos_stock_bajo: number;
}
